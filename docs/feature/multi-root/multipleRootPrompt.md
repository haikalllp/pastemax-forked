help me design a new feature for my program, essentially i want the program to now be able to accept multiple 'Root Folders', this should be possible with a new UI button off 'Add More' Next to or under the "Select Folder' button.

Essentially it should perform the same as select folder but allowing user to add more root folders into the project.

With this in mind copyable should also have correct sepration of concerns where user instructions should be at the very top and the rest of the content being at the bottom where each root folder is seperated. 

If chosen, file tree corresponding to the root should also show for each of the root folders seperated.

The rest of the functionality should remain the same.

For the UI strategically design a modern simple design that'll fit the current theme, and ensure it is compatible with dark and white theme.

Use sequential thinking to understand the code base, then develop a concrete plan using start_planning.